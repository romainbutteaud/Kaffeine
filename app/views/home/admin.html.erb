<% count = 0 %>
<% @sites.each do |site| %>
	<% count += 1 %>
	<%= "##{count} - "%>
	<%= site.created_at.strftime("%d %b. %Y - %H:%M") %>
	<%= link_to site.name, "http://#{site.name}.herokuapp.com" %><br>
<% end %>